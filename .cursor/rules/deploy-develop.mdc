---
description: Deploy automático após alteração de código — build + commit + push develop + produção
alwaysApply: true
---

# Deploy automático — NÃO esperar o usuário pedir

**Regra obrigatória:** Depois de **qualquer alteração de código** que o usuário pediu (fix, feature, UI, API, regra, etc.), o agente **sempre** faz deploy sozinho ao terminar — **sem** esperar o usuário escrever "deploy".

O usuário trabalha em **produção** e já pediu várias vezes: **não perguntar**, **não só avisar** — **executar**.

## Fluxo ao concluir implementação

1. **`pnpm build`** na raiz — obrigatório. Se falhar, corrigir e repetir.
2. **`git status`** → **`git add`** só arquivos relevantes (nunca temp, `.gradle`, `backup_clean.sql`, probes locais, etc.).
3. **`git commit`** — mensagem clara em pt-BR.
4. **`git push origin develop`**
5. **`git push production develop:develop`** — hook no Lightsail roda `deploy.sh` automaticamente (não precisa SSH manual).
6. **Responder** com hash do commit + confirmação do push (origin + production).

## Quando o usuário escrever "deploy" explicitamente

Mesmo fluxo acima. Não pedir confirmação extra.

## Exceções (não fazer deploy)

- Só pergunta/explicação, sem mudança de código.
- Usuário disse explicitamente **não subir** / **só local** / **não commitar**.
- Não há nada para commitar (working tree limpa).

## Branch e remotes

- Branch de trabalho: **`develop`**
- **`origin`** → GitHub (`rubinholima/bcg-site`)
- **`production`** → `bcg:/srv/git/bcg.git` (Lightsail `44.196.14.114`) · push **`develop:develop`**

## Servidor (só se push falhar ou rebuild sem código novo)

- Manual opcional: `~/deploy.sh` no Lightsail (ubuntu@bcg)
- Instalar hook (uma vez): `bash ~/bcg-site/infra/lightsail/install-server-deploy.sh`
- Secrets persistem em `~/bcg-site-shared/` (`.env` da API e Web)

## Padrão Atrium (referência)

Mesmo modelo do **ALT** (`ATRIUM-LIVE-TRANSLATOR`): bare repo `/srv/git/*.git`, hook `post-receive`, deploy verbose, push do PC = produção atualizada. Ver `infra/lightsail/` neste repo.
